CURL_CONFIG=/usr/local/bin/curl-config

# CURL_CFLAGS=$(shell $(CURL_CONFIG) --cflags)
# CURL_LIBS=$(shell $(CURL_CONFIG) --libs)

CURL_CFLAGS=-I/usr/local/include
CURL_LIBS=-L/usr/local/lib -lcurl -lssh2 -lssl -lcrypto -lldap -lz


# -DRCURL_DEBUG_MEMORY
DEFINES= -DHAVE_LIBIDN_FIELD=1

PKG_CPPFLAGS=$(CURL_CFLAGS) $(DEFINES)
PKG_LIBS=$(CURL_LIBS)

# enums.o: enums.c CurlOptEnums.h

